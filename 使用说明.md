# 清录屏使用说明

## 软件简介

清录屏是一款 Windows 桌面录屏工具，支持选择屏幕或窗口录制，也支持在预览画面中拖拽选择局部区域录制。软件提供 3 秒透明倒计时、录制中水滴悬浮窗、音频录制、MP4 视频导出和 GIF 动图导出。

## 主要功能

- 录制整个屏幕
- 录制指定窗口
- 拖拽选择局部区域录制
- 录制前透明全屏 3 秒倒计时
- 录制开始后自动收起主窗口
- 录制中显示水滴悬浮窗
- 水滴悬浮窗显示当前录制时长
- 水滴悬浮窗可点击结束录制
- 水滴内部波纹随实际收声音量起伏
- 支持录制内置声音
- 支持录制外部麦克风
- 支持不录音
- 支持导出 MP4 视频
- 支持导出 GIF 动图
- 内置 ffmpeg，用于 MP4/GIF 转码

## 使用流程

1. 打开清录屏。
2. 在左侧选择要录制的屏幕或窗口。
3. 如需局部录制，切换到 `局部` 模式，并在预览画面中拖拽选择区域。
4. 选择帧率、画质、声音来源和输出格式。
5. 点击 `开始录制`。
6. 等待透明倒计时结束。
7. 软件会自动收起，并显示水滴悬浮窗。
8. 录制完成后，点击水滴悬浮窗中的 `结束`。
9. 选择保存路径，生成 MP4 或 GIF 文件。

## 音频说明

声音来源有三种：

- `不录音`：只录制画面。
- `内置声音`：录制电脑正在播放的声音。
- `外部收声`：录制麦克风输入，可选择具体麦克风设备。

水滴悬浮窗的内部波纹会根据实际收声音量变化。如果选择 `不录音`，水滴会保留轻微待机波动。

## 输出格式说明

### MP4 视频

适合保存完整录屏、上传视频平台、发送给他人或后续剪辑。

如果当前 Electron 环境无法直接录制 MP4，软件会先录制临时 WebM 数据，再使用内置 ffmpeg 自动转码为真正的 MP4 文件。

### GIF 动图

适合录制短操作演示、界面反馈和动效预览。

注意：

- GIF 不包含声音。
- GIF 文件体积可能较大。
- 建议录制较短片段。

## 水滴悬浮窗

录制开始后，主窗口会自动收起，屏幕上会出现水滴悬浮窗。

水滴悬浮窗提供：

- 当前录制时长
- 结束录制按钮
- 实时音频波纹
- 拖动调整位置

点击 `结束` 后，录制会停止并进入保存流程。

## 打包与分发

生成目录版：

```powershell
npm run pack
```

生成安装包和便携版：

```powershell
npm run dist
```

常见输出文件：

```text
release/清录屏 Setup 1.0.0.exe
release/清录屏 1.0.0.exe
release/win-unpacked/
```

说明：

- `清录屏 Setup 1.0.0.exe` 是安装包，适合发给普通用户。
- `清录屏 1.0.0.exe` 是便携版，可以直接运行。
- `win-unpacked` 是目录版，必须保留整个文件夹，不能只单独拷贝里面的 exe。

## 分发注意事项

软件已经把 ffmpeg 打包进应用资源中，其他 Windows 电脑不需要额外安装 ffmpeg、Node.js 或 Electron。

建议优先分发：

```text
release/清录屏 Setup 1.0.0.exe
```

或：

```text
release/清录屏 1.0.0.exe
```

当前构建面向 Windows x64，适合大多数 Windows 10/11 电脑。

## 常见问题

### 点开始录制后没有进入录制

请检查：

- 是否已经选择录制来源
- 局部模式下是否已经框选区域
- 目标窗口是否仍然存在
- 音频设备是否被其他程序独占

### 内置声音录不到

请尝试：

- 切换为屏幕源后再录制
- 确认系统正在播放声音
- 关闭其他可能独占音频的程序
- 改用外部收声测试

### GIF 生成较慢

GIF 需要通过 ffmpeg 转码，较长录制会比较慢。建议 GIF 录制控制在较短时间内。

### 便携版打不开

请尝试：

- 将 exe 放到本地磁盘再运行
- 避免放在权限受限目录
- 检查杀毒软件是否拦截
- 改用安装包版本

## 开发命令

```powershell
npm install
npm start
npm run pack
npm run dist
```

